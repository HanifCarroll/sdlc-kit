# Blueprints

Issue blueprints are tactical execution plans for one issue at a time.

Generated `issue-*.md` files are local by default and ignored by git. Sync the useful execution summary back to the issue comment thread instead of committing local scratch plans.
