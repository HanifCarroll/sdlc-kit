# sdlc-kit CLI

The `sdlc` command installs and checks a repo-local SDLC contract for AI-assisted engineering.

This package is currently source-published for Bun. The first supported local path is:

```sh
bun run sdlc --help
```

The durable command surface is documented in the repository README while the implementation is being built.
