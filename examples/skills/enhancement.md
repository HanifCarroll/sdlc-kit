# Enhancement Example

```text
Use $sdlc-enhancement for this existing feature improvement:

The import review page already shows rejected rows, but it is hard to scan why each row failed. I want the existing table to group errors by reason and show a count per reason. Please create the GitHub issue first, clarify acceptance criteria, route through the right review path, and verify the UI behavior.
```
