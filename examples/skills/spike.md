# Spike Example

```text
Use $sdlc-spike for this research question:

Should we switch the background job runner from the current cron script to a queue-backed worker? Please create the spike issue first, define the decision criteria, inspect the current code, compare options, and finish with a recommendation plus follow-up implementation issues if the change is worth doing.
```

