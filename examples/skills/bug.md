# Bug Example

```text
Use $sdlc-bug for this bug:

The dashboard export button sometimes does nothing after a user filters the table. Expected behavior is that the export downloads the currently filtered rows. Actual behavior is that no file is downloaded and there is no visible error. Please create or update the GitHub issue first, investigate the root cause, add a regression test if feasible, and keep verification notes.
```
