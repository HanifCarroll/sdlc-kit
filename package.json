{
  "name": "sdlc-kit-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "adapters/*"
  ],
  "scripts": {
    "check": "bun run --filter '*' check",
    "test": "bun test",
    "lint": "bun run check",
    "sdlc": "bun run packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
